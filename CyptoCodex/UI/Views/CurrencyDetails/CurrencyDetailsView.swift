import SwiftUI

struct CurrencyDetailsView: View {
    var body: some View {
        Text("Currency Details").multilineTextAlignment(.center)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
        
    }
}

#Preview {
    CurrencyDetailsView()
}
